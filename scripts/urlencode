#!/usr/bin/env bash
# URL-encode input text using Python 3.

set -euo pipefail

if [[ $# -eq 0 ]]; then
  echo "Usage: urlencode <text>" >&2
  exit 1
fi

python3 -c 'import sys, urllib.parse; print(urllib.parse.quote_plus(" ".join(sys.argv[1:])))' "$@"

